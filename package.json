{
  "name": "ember-runtime-config",
  "private": true,
  "repository": {
    "type": "git",
    "url": "https://github.com/alexlafroscia/ember-runtime-config.git"
  },
  "license": "MIT",
  "author": "Alex LaFroscia",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "yarn workspace ember-runtime-config build",
    "prepare": "([ -n \"$CI\" ] || husky install) && yarn build",
    "test": "yarn workspace test-app test"
  },
  "devDependencies": {
    "@commitlint/cli": "^16.2.3",
    "@commitlint/config-conventional": "^16.2.1",
    "husky": "^7.0.4"
  },
  "engines": {
    "node": "14.* || 16.* || >= 18"
  },
  "volta": {
    "node": "14.21.1",
    "yarn": "1.22.19"
  }
}
